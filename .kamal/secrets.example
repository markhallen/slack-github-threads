# Example Kamal secrets configuration
# Copy this file to .kamal/secrets and update with your actual values

# Container Registry Password (for pushing Docker images)
# Get this from your container registry (GitHub Container Registry, Docker Hub, etc.)
KAMAL_REGISTRY_PASSWORD=your-container-registry-password

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required permissions: repo (for private repos) or public_repo (for public repos only)
GITHUB_TOKEN=ghp_your-github-personal-access-token

# Slack Bot Token
# Create at: https://api.slack.com/apps
# Required scopes: channels:history, groups:history, im:history, mpim:history, users:read, chat:write
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Alternative: Using 1Password CLI (replace with your actual values)
# CR_SECRETS=$(kamal secrets fetch --adapter 1password --account YOUR-ACCOUNT-ID --from your-vault/registry-token)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract token ${CR_SECRETS})

# SECRETS=$(kamal secrets fetch --adapter 1password --account YOUR-ACCOUNT-ID --from your-vault/app-secrets GITHUB_TOKEN SLACK_BOT_TOKEN)
# GITHUB_TOKEN=$(kamal secrets extract GITHUB_TOKEN ${SECRETS})
# SLACK_BOT_TOKEN=$(kamal secrets extract SLACK_BOT_TOKEN ${SECRETS})
